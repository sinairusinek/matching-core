# Cross-Vetting Ledger

Append-only record of changes to shared matching behaviour and their disposition
across services. See the process:
[entity-matching-skill/process/cross-vetting.md](https://github.com/sinairusinek/entity-matching-skill/blob/main/process/cross-vetting.md).

One row per triggering change. Never rewrite rows; add new ones.
Vetted column values: `adopt` / `adapt` / `reject` (+reason) / `n/a`.

| Date | Change | Originated in | Belongs in core? | Vetted: Places | Vetted: People | Vetted: Orgs | Status |
|------|--------|---------------|------------------|----------------|----------------|--------------|--------|
| 2026-05-27 | Seed core: normalize_name, is_hebrew, detect_script, trigrams, name_similarity, Kima/Wikidata id helpers, cascade skeleton, review-band routing | matching-core (extraction) | yes | adopt — normalizers.py is now a re-export shim; 26 tests pass | adapt — is_hebrew adopted; normalize_name stays domain (NFKD person pipeline) | pending packaging (Phase 3) | open |
| 2026-05-27 | `trigrams` promoted to top-level (was inline in Kimatch, private `_trigrams` in Shidduch) | matching-core | yes | adopt (Phase 2 ✓) | adopt — `_trigrams` now aliases core.trigrams (Phase 2 ✓) | n/a | closed |
| 2026-05-27 | RESIDUAL DIVERGENCE (logged, not yet resolved): Shidduch `normalize_name` keeps NFKD + paren-strip in its person pipeline vs core's NFD; and Shidduch `name_similarity` keeps a `len<4` short-string token fallback vs core's trigram-empty fallback. Kept to avoid score drift in an untested repo. | People (Shidduch) | yes (both should converge to core eventually) | n/a | TODO — delegate inner step to core.normalize_name once a People golden-test set exists | n/a | open |
| 2026-05-27 | Polish romanization digraphs in Latin→Yiddish translit: `sz→ש`, `szcz→שטש`, `cz→טש`, `rz→זש` (was degrading `Fiszon`→פֿיסזאָן and missing the match) | Orgs (Dybbuk `translit_latin_to_yiddish.py`) | yes (transliteration table) | adapt (Polish town spellings, e.g. Krzemieniec) | adopt (Polish-spelled surnames like Fiszon hit this directly) | n/a (origin) | open |
| 2026-05-27 | Head-noun-stripped alias for blocking person-named entities (drop the leading given name before a domain tail-word, so `<Surname>'s troupe` blocks against `Given Surname Troupe`) | Orgs (Dybbuk `prepare_alignment.py`) | yes (technique only — the org tail-word list טרופּע/קאָמפּאַניע/… stays Orgs-domain) | n/a (places aren't person-named) | adapt (People have given+surname natively; same surname-only reference problem) | n/a (origin) | open |
| 2026-05-27 | `token_jaccard` — promote the order-independent token-set Jaccard to a first-class similarity primitive and FUZZY-stage signal. Core already has this logic but only as a hidden fallback inside `name_similarity` (used when trigrams unavailable); make it primary so word-order/morphology variants ("troupe of X" ↔ "X's troupe") match. The org head-noun/of/article stripping that builds the token set stays Orgs-domain. | Orgs (Dybbuk `org_normalize.token_set_similarity`) | yes (the Jaccard primitive only) | adopt — shipped in core 0.2.0 as `matching_core.token_jaccard` | adopt with refinement — primitive in core; comma-aware wrapper (signals "Last, First" order) stays in Shidduch domain | adopt (origin; domain stripping stays) | closed |
| 2026-05-27 | `script_runs(text, script)` — extract the maximal runs of a given script from a mixed-script name string (built on `detect_script`); feeds per-script variant generation so a Yiddish query matches the Yiddish form embedded in a Latin-headed name. | Orgs (Dybbuk `prepare_alignment.split_name_variants`) | yes (the run-extraction primitive) | adopt — shipped in core 0.2.0 as `matching_core.script_runs` | adopt — shipped in core 0.2.0 | adopt (origin; the ' - '/parenthetical split conventions stay domain) | closed |
| 2026-05-27 | GRAY AREA for vetting: Yiddish possessive-`ס` / adjectival-`ער` / article (`דער/די/דאָס`) stripping — currently inside Orgs `token_key_set`. Is this shared Yiddish morphology (like the core geresh/nikud strip) that People also need, or domain? | Orgs (Dybbuk `org_normalize.token_key_set`) | yes, as OPT-IN (not in default cascade) | n/a (Latin/place script) | adopt as OPT-IN — shipped in core 0.2.0 as `matching_core.yiddish` (not auto-imported); Shidduch uses selectively, not unconditionally (surnames can legitimately end in ס/ער) | adopt (origin; service-specific data like org head-nouns + city-adjective map stays in Orgs) | closed |
| 2026-05-31 | matching-core 0.2.0 released: ships `token_jaccard` (sim), `script_runs` (normalize), opt-in `yiddish` module (articles, of-tokens, possessive-ס strip, adjectival-ער strip). 30 tests pass. Resolves vetting of rows 17–19. | matching-core (Phase 2.5) | yes | available — Kimatch can opt in to token_jaccard / script_runs in matcher.py | available — Shidduch can opt in (token_jaccard + comma-aware wrapper; script_runs; selective yiddish helpers) | available — Dybbuk org pipeline can replace its inline copies with core imports on Phase 3 packaging | open (adoption pending in each service) |
